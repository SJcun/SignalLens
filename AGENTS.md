# SignalLens Agent 协作规范

本文件适用于整个仓库。子目录如果以后增加更具体的 `AGENTS.md`，以距离目标文件最近的规则为准。

## 1. 项目目标

SignalLens 的核心不是通用摘要，而是 AI 阅读分诊：减少低价值信息带来的注意力消耗，同时保留新增知识、反方观点和跨领域探索价值。

所有功能优先回答以下问题：

1. 是否提高阅读分诊准确度？
2. 是否帮助用户决定跳过、摘要、选择性阅读或精读？
3. 是否保留低相关但高价值的内容？
4. 是否属于 SignalLens，而不是采集器、收藏系统或未来知识库？

## 2. 当前事实与边界

开始工作前先阅读：

- `README.md`
- `docs/README.md`
- `docs/AI内容筛选与消费助手_V0.3_防信息茧房与认知多样性方案.md`

当前已经完成：

- PageSift 派生网页采集插件；
- `signallens.capture.v1` 采集协议；
- FastAPI 内容采集、列表、详情和分析状态接口；
- SQLite 内容、分析和任务持久化；
- 同一内容类型与规范化 URL 的去重；
- Vue Inbox 和 Markdown 详情展示。

当前尚未完成：

- 三阶段 LLM 分析；
- Worker 任务消费和重试；
- 用户鉴权、插件 Token、画像、反馈和统计；
- PDF、音视频、RSS 等 Adapter。

不要把计划能力描述或实现成已经存在的能力。没有真实模型结果时，不生成占位 AI 结论。

## 3. 工作方式

实现前必须：

1. 检查 `git status --short`，保留用户已有改动；
2. 阅读目标代码和相邻测试；
3. 陈述关键假设、不同解读和更简单方案；
4. 定义可验证的成功标准；
5. 多步骤任务给出简短计划。

遇到会改变产品边界、数据语义或外部系统状态的不确定事项时，停止并询问。可以从代码或本地状态确认的事实，不要反问用户。

## 4. 简单与精准修改

- 只实现用户明确要求的能力；
- 不为一次性逻辑引入抽象；
- 不提前引入 Redis、Celery、Kafka、向量数据库、LangChain、LangGraph 或微服务；
- 不顺手重构、格式化或清理无关代码；
- 匹配现有代码风格；
- 删除仅因本次修改而变成无用的代码，不删除原有无关死代码；
- 每一行修改都应能追溯到当前需求。

## 5. 模块职责

### `apps/extension`

负责网页 Capture：正文提取、规范化、质量判断、Markdown/JSON 导出和主动提交。

- 保留 PageSift 的提取引擎、回退、选区和质量门禁；
- 网络提交放在 Manifest V3 service worker；
- API 和 Web 地址通过构建环境变量写入；
- 不在插件内调用 LLM；
- 不实现知识库或长期收藏；
- 禁止重新加入旧域名、Memento、旧账户、图片归档上传或 `/plugin-*` 接口。

### `apps/api`

负责内容身份、持久化、API 契约、任务状态和未来 AI 分析。

- FastAPI 请求与响应使用 Pydantic 模型；
- 数据访问使用 SQLAlchemy；
- API 路径保持 `/api/v1` 前缀；
- 慢速 LLM 调用不得阻塞采集请求；
- `POST /captures` 应快速可靠入库并返回 `202`；
- 手动采集不能因相关性低而被删除或隐藏。

### `apps/web`

负责 Inbox、内容详情、偏好、反馈和统计。

- 使用 Vue 3、TypeScript、Vue Router 和 TanStack Vue Query；
- 服务端状态交给 Vue Query，不为简单页面额外引入 Pinia；
- pending、running、completed、failed 都必须有明确界面状态；
- 被建议忽略的内容仍必须可见、可查看和可推翻。

## 6. 内容身份规则

- `capture_id` 标识一次采集请求，不是内容主键；
- 内容唯一身份是 `source_type + canonical_url`；
- Web URL 通过 `normalize_content_url` 移除 fragment 和常见追踪参数；
- 不要删除可能决定真实内容身份的未知查询参数；
- 重复提交更新现有正文快照，不创建第二条内容；
- 并发去重必须有数据库唯一索引兜底，不能只依赖应用层查询；
- 修改去重或迁移逻辑时，测试至少覆盖不同 `capture_id`、追踪参数、尾部斜杠和 fragment。

涉及现有数据清理时，先创建可恢复备份并核对精确目标。不要在未确认范围时删除数据库记录。

## 7. 时间规则

- 数据库统一保存 UTC；
- Python 使用带时区的 `datetime`，SQLite 返回 naive datetime 时按 UTC 补齐语义；
- API 必须输出带 `Z` 或明确 offset 的 ISO 8601 时间；
- Web 用户时间固定通过 `Asia/Shanghai` 展示北京时间；
- 不通过手工 `+8 小时` 处理时区；
- 测试必须能识别时区丢失问题。

## 8. AI 分析硬约束

未来实现 Prompt、Schema 和 Worker 时必须保持：

- `relevance` 低不等于内容价值低；
- 手动提交代表强用户意图，不静默过滤；
- 不确定时选择 `continue`，不是 `ignore`；
- 阅读动作与探索属性分开建模；
- `AnalyzeContent` 分析内容本身，不读取完整用户画像；
- 只有 `EvaluateForUser` 使用画像判断相关性和知识重叠；
- 不把“熟悉主题”误判为“已知所有知识点”；
- 不凭空制造反方观点，但原文已有的反对意见、限制、不确定性和未验证主张不能被摘要丢失；
- “推荐准确”默认只校准本次判断，不自动提高主题兴趣权重；
- 所有模型输出必须经过 Pydantic/JSON Schema 校验，并记录模型与 Prompt 版本。

## 9. 注释与语言

新增程序代码必须写清中文注释，至少覆盖：

- 类、函数和方法的用途；
- 重要字段的业务含义；
- 去重、迁移、任务领取、Prompt 约束等关键逻辑块。

注释说明意图和约束，不逐行复述语法。用户界面、项目文档和业务错误默认使用中文；协议字段、API 路径和标准技术名词保持英文。

## 10. 测试门禁

修改对应模块后，至少运行相关检查。

Web 或插件：

```powershell
npm run typecheck
npm test
npm run build
```

后端：

```powershell
Push-Location apps/api
uv run pytest
uv run ruff check src tests
Pop-Location
```

核心回归要求：

- 修复 Bug 时先增加能复现问题的测试；
- API 变更同步验证请求、响应和错误状态；
- 数据模型或迁移变更用临时数据库测试，并检查 Windows 文件句柄释放；
- 插件修改后验证构建产物、Manifest 权限和网络出口；
- 提交前运行 `git diff --check`；
- 不把 `node_modules`、`.venv`、`dist`、`.env`、SQLite 数据和备份提交 Git。

Defuddle 在 jsdom 中可能输出选择器兼容日志；以最终断言为准，但不要因此忽略真实失败。

## 11. 文档维护

- 根 `README.md` 只描述当前真实能力、运行方式和明确路线；
- `docs/README.md` 是文档总索引；
- 新功能、方案、审查和经验文档遵守 `docs/` 的编号与分类规则；
- 新建专题文档先进入 `docs/临时/`，得到用户认可后再归档；
- 方案和审查文档的执行状态必须与事实一致；
- 代码、命令或接口变化导致 README 失实时，应在同一任务中同步更新；
- 不修改用户原始方案结论，除非用户明确要求修订。

## 12. Git 与安全

- 不覆盖或回滚用户未提交改动；
- 禁止使用 `git reset --hard` 或未经确认的破坏性命令；
- 不提交 API Key、Token、密码和生产环境配置；
- 不把网页正文、浏览历史或用户数据发送到未声明的服务；
- 插件只在用户主动点击后提交正文；
- 不自动推送远程仓库或部署生产环境，除非用户明确要求。
