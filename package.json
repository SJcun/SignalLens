{
  "name": "signallens",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web",
    "apps/extension"
  ],
  "scripts": {
    "build": "npm run build --workspace @signallens/web && npm run build --workspace @signallens/extension",
    "typecheck": "npm run typecheck --workspace @signallens/web && npm run typecheck --workspace @signallens/extension",
    "test": "npm run test --workspace @signallens/web && npm run test --workspace @signallens/extension"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}

